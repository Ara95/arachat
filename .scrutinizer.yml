filter:
    excluded_paths: [client/public/*]

build:
    environment:
        mongodb: '3.6'
        node: v9.2.1
    tests:
        override:
            -
                command: 'npm run test-scrutinizer'
                coverage:
                    file: 'coverage/clover.xml'
                    format: 'clover'
